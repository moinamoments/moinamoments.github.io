{
  "name": "kassenpilot",
  "private": true,
  "version": "0.1.0",
  "description": "Mehrmandantenfaehiges Kassensystem (POS) fuer Android und iOS",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
